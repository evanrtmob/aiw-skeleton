services:
  nginx:
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d/default.prod.conf:/etc/nginx/conf.d/default.conf:ro

  backend:
    volumes:
      - ./backend/config.cfg:/code/app/config.cfg:ro
